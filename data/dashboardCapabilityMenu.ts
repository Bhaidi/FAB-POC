/**
 * @deprecated Static menu removed — use `buildCapabilityMenu` + `DashboardEntitlementsProvider`.
 * Re-exports shared types and `nav()` for deep links.
 */

export type { CapabilityAccess, CapabilityMenuItem } from "@/data/dashboardTypes";
export { nav } from "@/data/dashboardNav";
